export enum AppButtonTypes {
    Main = 'main',
    Secondary = 'secondary'
}

export enum AppButtonSizes {
    Small = 'small',
    Medium = 'medium',
    Huge = 'huge'
}

export enum MainContentAlignTypes {
    Left = 'left',
    Right = 'right',
    Center = 'center'
}